export const SOCKETIO_CONFIG = {
  domain: '{DOMAIN_OF_SOCKETIO_SERVER}',
  path: ''
}

export const AUTH0_CONFIG = {
  clientId: '{AUTH0_CLIENTID}',
  domain: '{AUTH0_DOMAIN}',
  callbackUrl: `${window.location.origin}`
}
